# ═══════════════════════════════════════════════════════════
#  SecureScore HQ — Python dependencies (Docker image)
# ═══════════════════════════════════════════════════════════
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.0,<3.0
apscheduler>=3.10,<4.0
numpy>=1.26,<2.0  # torch 2.2.x is ABI-incompatible with NumPy 2.x
xgboost>=2.1,<3.1  # 3.1+ base_score serialization breaks shap 0.49 TreeExplainer
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
slowapi>=0.1.9,<1.0

# ── Database ─────────────────────────────────────────────
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0        # PostgreSQL driver

# ── Observability ─────────────────────────────────────────
prometheus-client>=0.20,<1.0
prometheus-fastapi-instrumentator>=6.1,<7.0
opentelemetry-sdk>=1.24,<2.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-exporter-otlp>=1.24,<2.0

# ── MLflow experiment tracking ────────────────────────────
mlflow>=2.13,<3.0

# ── Certificate rotation ──────────────────────────────────
cryptography>=42.0,<44.0

# ── Testing (dev only) ────────────────────────────────────
# pytest>=8.0,<9.0
# pytest-cov>=5.0,<6.0
# httpx>=0.27,<1.0                 # for FastAPI TestClient async
# locust>=2.24,<3.0               # load testing
torch>=2.1.0
