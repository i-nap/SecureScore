apiVersion: 1

providers:
  - name: SecureScore
    orgId: 1
    folder: SecureScore
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
